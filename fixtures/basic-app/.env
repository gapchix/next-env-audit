# Committed on purpose — fixture values, nothing secret.
# CMS_TOKEN is deliberately NOT set: /static-secret must trigger the
# "prerendered with a missing env var" finding.
NEXT_PUBLIC_API_URL=https://api.fixture.example
BUILD_INFO=fixture-build-stamp
POSTS_SOURCE_URL=https://posts.fixture.example
